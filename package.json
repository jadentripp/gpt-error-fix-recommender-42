{
  "name": "gpt-error-fix-recommender-42",
  "displayName": "GPT-Error-Fix-Recommender",
  "description": "This extension uses the OpenAI API and GPT-3 model to summarize error messages that your console outputs. These recommendations may be incorrect and are provided for informational purposes only. BYOK (Bring Your Own Key) - you must provide your own OpenAI API key to use this extension.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:gpt-recommend-solution"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "gpt-recommend-solution",
      "title": "Recommend Solution"
    }]
  },
  "dependencies": {
    "openai": "^3.0.2",
    "vscode": "^1.74.0"
  },
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1"
  }
}
