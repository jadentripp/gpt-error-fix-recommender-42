{
  "name": "gpt-error-fix-recommender-42",
  "displayName": "GPT-Error-Fix-Recommender",
  "description": "This uses the OpenAI api and GPT3 model to summarize error messages that your console outputs. These may be wrong. These may be helpful. BYOK.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:gpt-error-fix-recommender-42.helloWorld"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "gpt-error-fix-recommender-42.helloWorld",
      "title": "Hello World"
    }, {
      "command": "gpt-recommend-solution",
      "title": "Recommend Solution"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "yarn run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "eslint": "^8.28.0",
    "glob": "^8.0.3",
    "mocha": "^10.1.0",
    "typescript": "^4.9.3",
    "@vscode/test-electron": "^2.2.0"
  }
}
